services:
  - type: static
    name: mongolian-whisper-frontend
    env: static
    buildCommand: npm install && npm run build
    staticPublishPath: dist
    envVars:
      - key: VITE_API_BASE
        value: https://mongolian-whisper-api.onrender.com
