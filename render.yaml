services:
  - type: web
    name: mongolian-whisper-frontend
    env: node
    plan: starter
    region: oregon
    buildCommand: |
      npm install
      npm run build
    startCommand: npx serve -s dist -l 10001
